/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    printWidth: 80,
    semi: true,
    singleQuote: true,
    arrowParens: 'avoid',
    tabWidth: 4,
    trailingComma: 'es5',
};

module.exports = config;
